{
  "name": "rules-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-gastos-diarios --config ../firebase.json 'vitest run'"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.17.1",
    "firebase-tools": "^15.27.0",
    "vitest": "^4.1.10"
  }
}
